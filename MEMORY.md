# MEMORY.md

Durable project-context notes for QuranicWords, kept in the repo (not in `.gitignore`) so the record survives across sessions and contributors. The full phased implementation plan lives outside this repo at `/home/rafi/.claude/plans/clone-the-full-project-serene-iverson.md` — this file is a shorter in-repo companion, not a replacement.

## What this is

QuranicWords is a Kotlin + Jetpack Compose + Material 3 app, MVVM + Hilt, Room, offline-first. Scope is deliberately narrow — **Quranic vocabulary learning only**, ordered by word frequency, with no alphabet stage and no grammar track. Content is being restructured from a flat module→lesson list into **chapter → section → lesson**, with section/chapter exams gating progression (80% pass threshold to unlock the next unit). There is no sign-in, no cloud backend, and no leaderboard — none of that exists in this codebase. Intended for real Google Play Store publish.

## Status

- **Phase 0 (foundation)** — done. Package set to `com.quranicwords.app`, `Qw` identifier prefix used throughout, the curriculum scoped to vocabulary-only from the start, Room DB at schema version 1 with no migration history to carry forward.
- **Phase 1 (chapter/section data model)** — done. `ChapterEntity`/`SectionEntity`/`LessonEntity.kind` (`REGULAR`/`LESSON_FLASHBACK`/`SECTION_EXAM`/`SECTION_FLASHBACK`/`CHAPTER_EXAM`/`CHAPTER_FLASHBACK`) replace the old flat `ModuleEntity`→`LessonEntity` shape. `tools/ingestion/11_build_curriculum.py` re-slices the 3,680-word corpus into 8 chapters × 10 sections × ~10-word lessons (887 lessons total, 17,540 exercises), interleaving lesson/section/chapter flashback checkpoints per the three-level design. Unlock/gating logic lives in the pure `CurriculumUnlockResolver` (unit-tested) plus `ProgressRepositoryImpl` (80% pass threshold on every non-`REGULAR` kind). `HomeScreen`/`HomeViewModel` render the chapter→section→lesson tree (functional, visual polish deferred to Phase 3).
- **Phase 2 (licensing & audio pipeline)** — done. Root `LICENSE`/`NOTICE` added (also bundled at `assets/NOTICE.txt`, surfaced in Settings → About → "Licenses & sources"). `tools/ingestion/12_segment_word_audio.py` fetches EveryAyah.com Alafasy verse audio + quran-align word-timing data (both CC BY 4.0) and ffmpeg-segments single-word clips — **proven on a 20-word sample (19 succeeded), not run at full scale yet** (1,582 of 3,680 words have the character-span data segmentation needs; scaling past the sample is a follow-up batch job). Sample clips are hosted at the public `rmrashahriar/QuranicWords-audio` repo's `word-audio-sample-v1` release. New `WordAudioRepository` (stream-on-demand + local-cache resolution) and `AudioBulkDownloadWorker` (WorkManager, backs the new Settings "download all audio" button/progress) are fully wired; `SfxPlayer` (`SoundPool`) plays 5 short UI sounds (correct/wrong/lesson-complete/exam-pass/streak-milestone) — self-synthesized via ffmpeg (fully original, no licensing question) rather than sourced, since a reliable CC0 download source wasn't available in this environment. Settings gained a master "Sound effects" toggle.
- **Phase 3 (UI/animation overhaul)** — done, with two items scoped down (noted below). New geometric eight-pointed-star mark (`ic_qw_mark.xml`/`ic_launcher_foreground.xml`, all launcher icon densities regenerated) replaces the old raster logo everywhere, with an idle glow pulse on `QwLogo`. "3D box" visual language formalized: `Elevation` tokens, shared `Modifier.pressDepth()`, `docs/UI_GUIDELINES.md`. Font-size setting (`FontScale`, 4 steps) scales the whole app via `LocalDensity`. `AnswerFeedbackOverlay` (green "Alhamdulillah!"/red "In-Sha-Allah, next time!", confetti on correct only) plus `SfxPlayer` wired into every check/match/mismatch in `LessonViewModel`. Matching exercise reworked: per-tile press-depth + selection pop + shake-on-mismatch, all via the new pressDepth modifier. New `ExerciseContent.TapWordInVerse` type (the "reverse direction" quiz - meaning shown, tap the matching word inside the verse) fully wired end-to-end and content-generated for all 1,582 span-confirmed words (`tools/ingestion/11_build_curriculum.py`). New chapter/section intro screens (`IntroScreen`/`IntroViewModel`) show precomputed word-count/occurrence/cumulative stats with staggered count-up reveals, reachable by tapping a chapter/section header on Home. New card-flip "story-fold" word browsing screen (`WordBrowseScreen`, `HorizontalPager` of `Qw3DFlipCard`s) as an on-demand alternate way to page through a section's words, reachable via a new icon next to each section on Home - deliberately additive, not a replacement of the structured in-lesson teach step. Splash screen gained an opening chime (self-synthesized, like the other SFX) and a rotation-in on the logo. Verse-reference completeness audit: found and fixed one real gap (`FillInTheBlank` had no reference field at all - added `sentenceReference`); `WordOrderBuilder`'s constructed phrase was left flagged rather than fixed, since it has zero real seeded content today and it's unclear it represents a citable single verse.
  - **Scoped down / deferred, noted honestly rather than silently skipped**: the Lottie/Rive splash and streak-flame assets are still unused (no real `.json`/`.riv` file sourced - a reliable, license-clean way to fetch one wasn't available in this environment; the vector star + glow + rotation covers the "opening animation" requirement without it). "Excessive animation everywhere" was applied to the screens touched this phase (feedback overlay, matching, intro, word-browse, splash) but not swept across every remaining screen (e.g. Settings, LessonSummary) - a reasonable follow-up, not a blocker.
- **Phases 4-6** — not formally started as separate passes, but substantially covered as part of Phase 3's work (Alhamdulillah/In-Sha-Allah copy already threaded through the new feedback/intro strings in both languages; licensing/Play-Store-readiness items tracked in the plan file's Phase 6 checklist, still need a real completion pass before submission).

Verified after every phase: `./gradlew :app:compileDebugKotlin`, `:app:testDebugUnitTest`, `:app:assembleDebug` all pass.

## Key decisions (see the plan file for the complete table and rationale)

- Chapter/section split: 8 chapters × ~10 sections, contiguous frequency-rank slices, never reordered.
- Exam pass threshold: 80%, at lesson/section/chapter flashback and exam levels alike.
- Word audio: single-word clips segmented from EveryAyah.com (Alafasy) + quran-align timing, both CC BY 4.0; streamed on demand by default, with a Settings bulk-download-to-device option.
- Content licensing: NOTICE file credits Quranic Arabic Corpus (GPL), Quran-bil-Quran (MIT), risan/quran-json (CC BY-SA 4.0); app's own code is proprietary/all-rights-reserved, `Copyright © rmrashahriar 2026`.
- Mid-stream additions folded into the plan's addendum: reverse-direction (meaning-shown, Arabic-blanked) quiz variants, gradually increasing cloze difficulty, three-level flashback review exams (lesson/section/chapter, each scoped to earlier siblings under the same parent).

## Working agreements

- Keep this file and `CLAUDE.md` tracked in git — never add either to `.gitignore`.
- Commit and push work to `git@github.com:rmrashahriar/QuranicWords.git` regularly rather than letting it sit local-only; cut a GitHub release after each clean build milestone.
