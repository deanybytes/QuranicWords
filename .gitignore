*.iml
.gradle
.kotlin
/local.properties
# Claude Code local session/task state - not project content
.claude/
# Whole IDE project directory - embeds local machine paths/state, not project content
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Secrets - never commit real credentials or signing material
app/google-services.json
*.jks
*.keystore
keystore.properties
signing.properties
.env
.env.*

# Ingestion pipeline scratch data (scraped HTML, intermediate JSON) - reproducible by re-running
# tools/ingestion/*.py; the actual deliverable is app/src/main/assets/content/*.json, which is
# tracked normally. Keeping these out of git avoids ~38MB of regenerable intermediate data.
tools/ingestion/raw/
tools/ingestion/output/
tools/ingestion/__pycache__/

.tmp_gtaf_collection/
